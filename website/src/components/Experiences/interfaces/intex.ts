export interface IExperience {
  dateFrom: string;
  dateTo?: string;
  title: string;
  company: string;
  description: string;
};

export interface IExperienceDescription {
  company: string;
  description: string;
};
